{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Self Test Manager Config Schema",
    "description": "Defines the structure for a self_test_manager config.json entry.",
    "type": "object",
    "properties": {
        "type": { "const": "self_test_manager" },
        "enabled": { "type": "boolean" },
        "config": {
            "type": "object",
            "properties": {
                "instance_name": { "type": "string", "minLength": 1 },
                "storage_service_instance": { "type": "string", "minLength": 1 }
            },
            "required": ["instance_name", "storage_service_instance"],
            "additionalProperties": false
        }
    },
    "required": ["type", "config"]
}