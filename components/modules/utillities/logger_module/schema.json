{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Logger Module Config Schema",
    "description": "Defines the structure for a logger_module config.json entry.",
    "type": "object",
    "properties": {
        "type": { "const": "logger_module" },
        "enabled": { "type": "boolean" },
        "config": {
            "type": "object",
            "properties": {
                "instance_name": { "type": "string", "minLength": 1 }
            },
            "required": ["instance_name"],
            "additionalProperties": false
        }
    },
    "required": ["type", "config"]
}