{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Device Identity Service Config Schema",
    "description": "Defines the structure for a device_identity_service config.json entry.",
    "type": "object",
    "properties": {
        "type": { "const": "device_identity_service" },
        "enabled": { "type": "boolean" },
        "config": {
            "type": "object",
            "properties": {
                "instance_name": { "type": "string", "minLength": 1 }
            },
            "required": ["instance_name"],
            "additionalProperties": false
        }
    },
    "required": ["type", "config"]
}